# decision-log.md - 결정 사항 기록장

## 2026-05-13

### cost-sim-v2.1 게임화 — 1차수 D+14 (16개 결정 박제)

- **Scope**: `projects/cost-sim-v2.1/` 전체 (워크시트 UX 개편 + Sandbox 통합 + 게이미피케이션 백엔드)
- **결정 트리 (16개)**:
  1. **운영 규모**: 30명 × 6차수, LG Display 사내 강의실, 회사 노트북 동시 접속
  2. **게이미피케이션 모델**: α 실시간 리더보드 (서버 공유 상태 필수)
  3. **학습자 식별**: 룸 코드 + 자율 입력 (이름 + 팀 번호 직접). 강사 뷰에서 팀 수 동적 설정 (기본 5팀 × 6명). 팀 오타는 강사 대시보드에서 수정
  4. **타이머 정책**: 강사 신호 모델 (룸 전체 동시 출발). 라운드별 진행 (4 문제 = 4 라운드)
  5. **시간 산정**: 문제별 풀이 시간 합산 (A — 강사 신호로 라운드 시작 → 100% 정답 도달 시각 차이)
  6. **시간 캡**: 10분/문제 (기본값, 강사 설정에서 조정 가능)
  7. **라운드 종료**: 100% 정답 자동 종료 (X) — 채점-틀린셀초기화-재계산-채점 반복하다 모든 yellow 정답인 순간 자동
  8. **미완료자**: 캡 시간(10분)으로 합산 (m1) — 게임 끝까지 참여, 자연스럽게 하위 정렬
  9. **팀 시간 계산**: 라운드별 max 합 (I) — 매 라운드 가장 늦은 조원 시간을 팀 시간으로, 4 라운드 합산
  10. **라운드 사이 마디**: 매 라운드 즉시 공개 + 강사 코멘트 (P) — 강사 신호로 다음 라운드 시작
  11. **결과 공개 범위**: 학습자 화면에도 전체 리더보드 (α2)
  12. **포상 카테고리**: 개인 1·2·3 + 팀 1·2·3 (가). 보조 카테고리 시스템 강제 미적용 (강사 재량). 독식 우려는 무시
  13. **Sandbox Multi-factor**: 7 변수 (Loading·재료비·수율·면취수·Mask·Tact·투자상각비), 면취수·Mask는 새값만 슬라이더(기준값 reference 고정). Multi-open accordion 기본 모두 닫힘. 케이스 드롭다운 완전 제거 (γ)
  14. **워크시트 UX**: 계산기 sticky bottom 항상 표시 (A1) + 토큰 모델 유지 + 자유 텍스트 입력 병행 (B1) + % 자동 변환. 단위 통일 ($/%) + 총수율 행 추가. LLM 코치 코드 완전 제거
  15. **힌트**: 셀별 60 hints 제거 → 문제 단위 3단계 12 hints (b). 힌트 차감 100/70/40/20% 유지하되 *강사 설정에서 토글 가능*. 12개 힌트의 셀별 적절성 검증 필수
  16. **백엔드**: AWS Amplify SSR + DynamoDB + 폴링 2초 (a). Firebase/Supabase는 사내망 차단 가능성으로 배제
- **흐름 / 라우트**: `/` 룸코드 → `/intro` 6비트 다이얼로그 그대로 (iii) → `/menu` (Sandbox / 게임 모드). 게임 모드 = 기존 `/cases/[id]` 위에 룸 컨텍스트 분기 (신규 라우트 안 만듦). `/instructor` 별도 + admin_token (A)
- **다시풀기**: 틀린 셀만 초기화 (맞은 셀 유지). 채점 결과는 O/X만, 정답 숫자 미노출
- **강사 설정 패널 일반 원칙**: 가변 파라미터(시간 캡·팀 수·힌트 차감·발표 카테고리)는 모두 강사 뷰 설정에서 조정 가능
- **부정행위 방지**: 시간 timestamp는 Lambda 서버 시각 사용 (학습자 노트북 시계 조작·새로고침으로 시간 리셋 불가)
- **작업 순서**: B(워크시트 UX) → A(Sandbox 통합) → C(게이미피케이션). Phase 0 cleanup이 먼저
- **일정**: 1차수 D+14 = 2026-05-27 고정. Plan A 채택 (전부 2주에). AI 가속 + 풀집중 베팅. D-2(D+12) 리허설 시점에 게이미피케이션 동작 여부 판단 후 Plan B fallback 게이트 (워크시트만 + 수동 운영)
- **정밀도**: 작업 정밀도 절단 없음. 22~28일 추정 작업을 D+14 안에 압축
- **이유**:
  - 학습자가 *압도적으로 게임 동기*를 받도록 6차수 일관 운영
  - 명세에 명시된 모든 항목(워크시트 UX 6개 + Sandbox 7변수 + 게이미피케이션 룰)을 1차수 학습자에게도 제공한다는 약속
  - 사내망 차단 위험 회피 위해 기존 운영 검증된 Amplify 트랙 선택
- **영향 범위**:
  - `projects/cost-sim-v2.1/` 전반 (60% 이상의 파일 신규/수정)
  - AWS Amplify 환경변수 (GEMINI_API_KEY 제거, AWS_REGION/DYNAMODB_TABLE_NAME 추가)
  - DynamoDB 신규 테이블 1개 (single-table design)
  - 기존 4 case JSON의 cell·row hints 필드 60개 제거 (git history에 보존)
  - 라이브 URL `https://master.d26yr76roz76fk.amplifyapp.com/`는 그대로 유지
- **거부된 대안**:
  - **Firebase/Supabase 백엔드** — 사내망 차단 가능성. AWS 트랙으로 회피
  - **WebSocket / AppSync 실시간** — 30명·10분 라운드 척도에서 폴링 2초 vs <500ms 차이 체감 불가. 작업량 1주 더 들어가는 만큼 일정 위험 ↑
  - **Plan B 단계 출시 (1차수에 B만 + 수동 운영)** — 사용자가 명세 완성도를 1차수부터 약속, Plan A 채택
  - **개인 1·2·3 독식 방지 보조 카테고리 시스템 강제 (라운드 영웅·히든 솔버·점프상)** — 사용자가 독식 우려 무시 결정
  - **WebSocket 재연결 / 정교한 부정행위 방지** — 작업량 vs 30명 규모 비교, 폴링 + 서버 timestamp로 충분
  - **케이스 드롭다운을 시나리오 프리셋 버튼으로 보존 (δ)** — γ 완전 제거 선택. 4 케이스 학습 가치는 워크시트에서 다룸

---

## 2026-05-12

### cost-sim-v2.1 케이스 로스터 4종으로 확정

- **Scope**: `projects/cost-sim-v2.1/`
- **결정**: Case 01-loading, 04-material-yield, 05-cuts-mask, 06-tact-investment 4개를 정전(canon)으로 둔다. 원본 엑셀 번호 그대로 유지(리넘버 X).
- **이유**:
  - v2.1은 v1/v2-game의 6-케이스 + 4-phase Guided 모델에서 **Sandbox + Worksheet 2-동선 모델**로 피벗 — 워크시트 채점·3단계 힌트 시스템의 마감 깊이가 케이스 수 확장보다 우선.
  - 빠진 두 메커니즘(인건비 변동 → SGA 30% 비중 / 한계이익률 역산)은 *개념*으로는 Sandbox 슬라이더·인스펙터에 여전히 노출됨.
- **영향 범위**:
  - `context/glossary.md` "6개 케이스 요약" → "4개 케이스 요약"으로 정리됨
  - `projects/cost-sim-v2.1/README.md` 6-케이스/Guided 4-phase 표현 → 4-케이스/Sandbox+Worksheet로 정리됨
  - 향후 케이스 추가 요구 발생 시 본 결정을 supersede하는 새 결정 필요
  - 신규 케이스 추가 SOP: `content/cases/*.json` + `content/problems/p*.json` + `content/case-adapters/index.ts` + `lib/cases.ts`(CASES/CASE_ORDER) 4곳 동시 갱신
- **거부된 대안**:
  - Case 02·03 콘텐츠 복원 — 워크시트 마감 우선이라 보류
  - 케이스 번호를 1/2/3/4로 연속 리넘버 — 엑셀 원본 traceability 손실

## 2026-04-09

### 프로젝트 폴더 구조 확정

- **결정**: 4대 기본 폴더(`context/`, `projects/`, `templates/`, `outputs/`) + 통제 레이어(`.claude/`) 구조 채택
- **이유**: 클로드가 읽을 배경지식(Context), 지킬 규칙(Harness), 기록할 상태(State)를 명확히 분리하기 위함
- **영향 범위**: 모든 향후 작업은 이 구조를 따름
