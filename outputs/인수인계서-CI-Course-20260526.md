# CI-Course 인수인계서

> **작성일**: 2026-05-26 (최종 수정: 2026-06-08)
> **작성자**: Sam Ahn
> **인수 대상**: 개발자 / 강의 운영자 (복수 인수 가능)
> **상세 기술 문서**: `projects/cost-sim-v2.1/docs/handover-sim-v2.1.md` (825줄)
> **신규 AWS 계정 배포 가이드**: `projects/cost-sim-v2.1/docs/new-account-deploy-guide.md`

---

## 1. 한눈에 보기

- **프로젝트 정체**: LG Display CI(원가혁신) 과정 학습자용 **개발원가 산정 시뮬레이션 게임**
- **현재 버전**: `projects/cost-sim-v2.1/` (활성)
- **신규 라이브 URL**: https://master.d3arok48ln7jvp.amplifyapp.com ← **현재 운영 중**
- **구 라이브 URL**: https://master.d26yr76roz76fk.amplifyapp.com (Sam 구 계정 — 폐기 예정)
- **GitHub 저장소**: https://github.com/D-W-Kim/CI-Course (`master` 브랜치 자동 배포)
- **원본 저장소 (참조용)**: https://github.com/Koolsangon/CI-Course
- **백엔드**: AWS Amplify SSR (WEB_COMPUTE) + DynamoDB
- **전체 운영 계획**: 6차수 × 30명 = 누적 180명 (중형·대형 개발 그룹 위주, 5차수 내외)
- **기술 스택**: Next.js 14 · React 18 · TypeScript · Zustand · Tailwind · AWS

---

## 2. 교육 과정 개요

### 배경 및 목적

CI 과정은 LG Display **PL급 R&D 연구개발직**을 대상으로 합니다.

- **목적**: 엔지니어가 설계를 넘어 **제품 전체의 원가와 수익성을 책임지는 관점** 갖기
- **메시지**: 원가 절감 부담이 아닌 **"우리가 기술로 회사를 책임진다"는 자부심** 고취
- **타깃**: 중형 개발 그룹(~60명) + Auto(~40명) 우선 → 대형 개발 → SC 순 확장
- **제조 부문**: 당장 진행 안 함 (동진님과 추가 조율 예정)

### 과정 구성 (3 모듈)

- **Module 1** — 마인드셋 및 위기 인식 (강사: 최기성) — 대본이 길어 과감히 축소 필요
- **Module 2** — 원가 수익성 케이스 스터디 (강사: 박지영) — 날카로운 성향, 설득 필요
- **Module 3** — 웹 기반 원가 시뮬레이션 (강사: 남수미 운영 리드) — 재경 부서 출신, 이해도 높음

**현장 운영 지원**: 현이님 전담
**리더(병호님)**: 딜리버리(전달력) 과정에서 문제·불만 없도록 운영하는 것이 핵심

### 영상 자료

- **오프닝 영상**: 2분 10초, 컷 편집(근만님) → 자막 추가(상율님)
- **양치기 소년 영상**: 위기에 무덤덤해진 내부 조직 환기용 (사용 합의 완료)
- **경찰 아들 영상**: 집중도 깰 수 있어 **쉬는 시간에만 틀거나 제거**
- **CEO 엔딩 영상**: 공수·보고 절차 부담으로 **최종 취소**

---

## 3. 시뮬레이션 (Module 3) 개요

### 무엇을 하는 도구인가

LG Display 생산 현장 CI 과정 교육생이 **개발원가 계산 로직**을 손으로 익히는 학습 게임입니다.

- 기존: 엑셀 노란 셀에 답 입력 → 채점
- 현재: 웹 기반 워크시트 + 실시간 타임어택 게임 + 팀 경쟁
- 보안: 실제 로직은 압축·단순화된 뼈대 사용. **교육 차수에만 URL 오픈**, 종료 후 차단

### 게임 구조

- **4 라운드 × 10분 캡** 타임어택
- **4개 케이스** (원가 변동 메커니즘별):

| 케이스 ID | 주제 |
|-----------|------|
| `01-loading` | Loading율 변동 (가동률) |
| `04-material-yield` | 재료비 절감 vs 수율 하락 |
| `05-cuts-mask` | 면취수 · Mask 복합 효과 |
| `06-tact-investment` | Tact 지연 + 투자비 |

- **개인 순위**: 4 라운드 풀이 시간 합산 1·2·3등
- **팀 순위**: 팀에서 가장 늦은 조원 시간 4 라운드 합산 1·2·3등
- **정답**: O/X만 표시 (숫자 미노출)
- **힌트**: 문제 단위 3단계 (100/70/40/20% 차감, 강사 토글)

---

## 4. 현재 개발 상태

### 진행도: 14/17 코드 완료 (2026-05-26 기준)

- ✅ 코드 완료 (14): 워크시트, Sandbox, 게임 룸, 강사 뷰, API 5종, DynamoDB adapter
- ⏳ HITL 필요 (3): DynamoDB 환경변수 설정 + Amplify 재배포, 사내망 검증, 실제 강의 운영

### 최신 변경 사항

- **DynamoDB 연동 코드 완성**: `lib/room/storage-dynamodb.ts` 신규
  - `DYNAMODB_TABLE_NAME` 환경변수 설정 시 자동 DynamoDB 사용
  - 미설정 시 기존 in-memory 동작 (로컬 dev, 테스트 그대로 통과)
- **신규 AWS 계정 배포 완료** (2026-06-08): `https://master.d3arok48ln7jvp.amplifyapp.com`
- **typecheck 0 에러 / vitest 92/92 통과** 유지

---

## 5. 남은 핵심 작업 (인수자 최우선)

### 🚨 1순위 — DynamoDB 활성화 확인

코드와 테이블은 완성됨. 아래 명령어로 데이터가 실제로 쌓이는지 확인하세요.

```bash
# 방 생성 테스트
curl -X POST "https://master.d3arok48ln7jvp.amplifyapp.com/api/rooms" \
  -H "Content-Type: application/json" \
  -d '{"settings":{"timeCapSec":600,"teamCount":5,"hintPenaltyEnabled":true,"announcementMode":"full"}}'
# → {"code":"XXXX","adminToken":"..."}

# DynamoDB 콘솔에서 데이터 확인
# AWS 콘솔 → DynamoDB → Tables → cost-sim-rooms → Explore items
```

### 2순위 — D-2 셀프 리허설

- 사내 노트북 1대로 라이브 URL 접속 확인
- 강사 + 학습자 2탭 시뮬레이션
- **미동작 시 Plan B**: 워크시트만 + 강사 수기 시간 측정

### 3순위 — S5 엑셀 원본 확보

총수율 행 위치·표 순서·단위 표기 엑셀 원본 → `context/` 또는 `docs/`에 배치

---

## 6. AWS 인프라 현황 (신규 계정 기준)

> 2026-06-08 새 AWS 계정으로 완전 이전 완료.
> 상세 배포 가이드: `projects/cost-sim-v2.1/docs/new-account-deploy-guide.md`

### 현재 신규 계정 설정값

- **AWS 계정 ID**: `629015357851`
- **IAM 사용자**: `behappy6965` (AdministratorAccess)
- **Amplify 앱 ID**: `d3arok48ln7jvp`
- **리전**: `ap-northeast-2` (서울)
- **라이브 URL**: `https://master.d3arok48ln7jvp.amplifyapp.com`
- **GitHub 연결**: `D-W-Kim/CI-Course` (master 브랜치 push → 자동 배포)

### Amplify 앱 환경변수

| 변수명 | 값 | 비고 |
|---|---|---|
| `AMPLIFY_MONOREPO_APP_ROOT` | `projects/cost-sim-v2.1` | 모노레포 루트 지정 (필수) |
| `DYNAMODB_TABLE_NAME` | `cost-sim-rooms` | DynamoDB 테이블 이름 |
| `AWS_REGION` | (자동 주입) | Lambda 런타임이 자동 설정 — 직접 입력 불필요 |

> ⚠️ `AWS_`로 시작하는 변수는 AWS 예약어라 Amplify에서 직접 입력 불가. `AWS_REGION`은 Lambda가 자동으로 `ap-northeast-2`를 주입함.

### Amplify 빌드 설정 (`amplify.yml`)

```yaml
version: 1
applications:
  - appRoot: projects/cost-sim-v2.1
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
```

### DynamoDB

- **테이블 이름**: `cost-sim-rooms`
- **리전**: `ap-northeast-2`
- **빌링 모드**: PAY_PER_REQUEST (사용한 만큼만 과금)
- **ARN**: `arn:aws:dynamodb:ap-northeast-2:629015357851:table/cost-sim-rooms`

### IAM 역할 (Amplify SSR Lambda용)

- **역할 이름**: `AmplifySSRLambdaRole`
- **정책**: `CostSimDynamoDBPolicy` (DynamoDB GetItem/PutItem/UpdateItem/DeleteItem/Query)
- **정책**: `AWSLambdaBasicExecutionRole` (CloudWatch 로그)

### 주의사항 — 신규 계정에서 Amplify 앱 재생성 시

기존 계정에서 성공한 방법과 **다른 부분**이 있습니다. 반드시 아래를 따르세요:

1. **프레임워크 선택 화면에서 "없음"으로 두지 말고 `Next.js - SSR` 선택** (또는 CLI로 `update-branch --framework "Next.js - SSR"`)
2. **환경변수 `AMPLIFY_MONOREPO_APP_ROOT=projects/cost-sim-v2.1` 반드시 추가** (없으면 빌드 즉시 실패)
3. **`AWS_REGION`은 입력하지 말 것** (AWS 예약어라 오류 발생)
4. **Platform: `WEB_COMPUTE`** 반드시 선택 (WEB으로 설정 시 API 라우트 미동작)

### GitHub Fork 방식

Sam님의 구 계정(`Koolsangon`)에서 신규 계정(`D-W-Kim`)으로 Fork하여 운영합니다.

- 원본 레포: `https://github.com/Koolsangon/CI-Course` (참조용)
- 운영 레포: `https://github.com/D-W-Kim/CI-Course` (이 레포에 push → Amplify 자동 배포)

---

## 7. 빠른 요약 — 신규 AWS 계정 배포 (8단계)

```
0. AWS 계정 신규 생성 — 본인 이메일 + 결제 카드 등록
1. 사전 준비 — Node.js 20, AWS CLI, Git
2. GitHub Fork — github.com/Koolsangon/CI-Course → 새 계정으로 Fork
   → git clone https://github.com/[새계정]/CI-Course.git
3. DynamoDB 테이블 생성
   aws dynamodb create-table --table-name cost-sim-rooms \
     --attribute-definitions AttributeName=PK,AttributeType=S AttributeName=SK,AttributeType=S \
     --key-schema AttributeName=PK,KeyType=HASH AttributeName=SK,KeyType=RANGE \
     --billing-mode PAY_PER_REQUEST --region ap-northeast-2
4. Amplify 앱 생성
   → Platform: WEB_COMPUTE
   → 프레임워크: Next.js - SSR
   → 브랜치: master
5. amplify.yml — 레포 루트에 이미 포함 (자동 감지)
6. IAM 역할 설정 — AmplifySSRLambdaRole + CostSimDynamoDBPolicy
7. 환경변수 설정 (2개만)
   AMPLIFY_MONOREPO_APP_ROOT = projects/cost-sim-v2.1   ← 필수!
   DYNAMODB_TABLE_NAME = cost-sim-rooms
8. 첫 배포 → 라이브 URL 확인 → 강의 운영팀 공유
```

---

## 8. 시스템 접근 정보

### 개발 환경 셋업

```powershell
cd "D:\02. AI\02. Claude\Vibe Coding\28. CI 과정 시뮬레이션 개발\CI-Course\projects\cost-sim-v2.1"
npm ci
npm run dev
# → http://localhost:3000
```

### 핵심 명령어

```bash
npm run typecheck   # 타입 검사 (0 에러 기준)
npm test            # 단위 테스트 (92/92 기준)
npm run build       # 프로덕션 빌드
npm run dev         # 개발 서버
npx playwright test # E2E (시스템 Chrome 자동 사용)
```

### AWS 자격증명

- **IAM 사용자**: `behappy6965` (AdministratorAccess)
- ⚠️ Access Key는 별도 채널로 전달. 절대 Git 커밋 금지
- `.gitignore`에 `*_accessKeys.csv` 등록됨

### 주요 서비스 계정

| 서비스 | 접근 | 비고 |
|--------|------|------|
| GitHub | `D-W-Kim/CI-Course` | 운영 레포 (push → 자동 배포) |
| AWS Amplify | 앱 ID: `d3arok48ln7jvp` | 콘솔 접근 |
| AWS DynamoDB | 동일 AWS 계정 | `cost-sim-rooms` 테이블 |

---

## 9. 강의 운영 방법

### 강의 1회 진행 흐름 (총 2시간)

| 단계 | 시간 | 작업 |
|------|------|------|
| 사전 준비 | -10분 | `/instructor` → 새 방 생성 → 코드(4자) 칠판 기재 |
| 설정 | -5분 | 시간 캡 10분, 팀 수 5, 힌트 차감 ON 확인 |
| 학습자 입장 | 0~10분 | 30명 각자 노트북 → URL 접속 → 코드+이름+팀 입력 |
| 퀴즈 + 설명 | 10~40분 | 초반 30분 퀴즈 풀이 및 게임 방식 설명 |
| 10분 휴식 | 40~50분 | - |
| 라운드 1~4 | 50~90분 | 강사 Start → 자동 진입 → 10분 캡 → 결과 발표 반복 |
| 종합 발표 | 90~100분 | 강사 End Game → 개인/팀 1·2·3등 자동 표시 |
| 디브리프 | 100~120분 | Sandbox 자유 탐험 + 학습 정리 |

**수익성 1·2등 팀 보상** 있음 (강사 재량)

### Plan B (DynamoDB 미동작 시)

1. 워크시트 URL 직접 공유 (`/cases/01-loading` 등)
2. 강사 수기 시간 측정
3. 결과 구두 발표

---

## 10. 주의사항

### ⚠️ 절대 금지

- `.env`, AWS 키 파일 → 절대 Git commit 금지
- `lib/cost-engine/` → 임의 수정 금지 (27개 골든 픽스처 회귀 필수)
- `git push --force` 금지

### 🔧 자주 발생하는 문제

| 증상 | 원인 | 해결 |
|------|------|------|
| dev server 오류 | HMR 캐시 오염 | `rm -rf .next && npm run dev` |
| Playwright 설치 실패 | 사내망 TLS 차단 | 시스템 Chrome 자동 사용 (설정 완료) |
| 라이브 룸 데이터 사라짐 | DynamoDB 환경변수 미설정 | `DYNAMODB_TABLE_NAME` Amplify 설정 |
| 사내 노트북 URL 불가 | 방화벽 | `*.amplifyapp.com` 화이트리스트 요청 |
| Amplify 빌드 즉시 실패 | `AMPLIFY_MONOREPO_APP_ROOT` 미설정 | 환경변수 추가 후 재배포 |
| Amplify 빌드 실패 (deploy-manifest) | Platform이 WEB 또는 프레임워크 미설정 | WEB_COMPUTE + Next.js - SSR로 변경 |
| API /api/rooms 404 | Platform이 WEB(정적)으로 설정됨 | 콘솔에서 WEB_COMPUTE로 변경 후 재배포 |

---

## 11. 파일 구조 (핵심만)

```
CI-Course/
├── CLAUDE.md              ← 운영 계약 (필독)
├── CONTEXT.md             ← 도메인 인덱스
├── amplify.yml            ← Amplify 빌드 설정 (레포 루트)
├── plan.md                ← 작업 계획 17 slices (14 완료)
├── handoff.md             ← 세션별 작업 로그 (최신순)
├── decision-log.md        ← 결정 기록
├── outputs/               ← 최종 산출물
│   ├── 인수인계서-CI-Course-20260526.md  ← 이 문서
│   ├── cost-sim-v2.1-game-prd.md
│   └── 개발원가-War-Game-설명서.pdf    ← 학습자 배포물
└── projects/
    └── cost-sim-v2.1/     ← ★ 활성 버전
        ├── lib/room/
        │   ├── storage.ts            ← 진입점 (env 분기)
        │   └── storage-dynamodb.ts   ← DynamoDB 구현
        └── docs/
            ├── handover-sim-v2.1.md          ← 상세 기술 인수인계서
            ├── dynamodb-setup.md             ← DynamoDB 설정 가이드
            └── new-account-deploy-guide.md   ← 신규 계정 배포 가이드
```

---

## 12. 행정 및 예산

- **예산**: 6, 7월 과정 개발·차수 운영 예산 이미 RP 반영 완료
- **고용보험 환급**: 1차 파일럿은 제외, **2차수부터 환급 적용**
- **문서 오너십**: 기존 교재 편집 권한 + 관련 문서 모두 상율님(또는 팀장님)에게 이관
- **파일럿 일정**: 6/12(금) 오전 선호 (경영관 305호 예약 완료)

---

## 13. 인수 체크리스트

### Day 1 (인수일)

- [ ] 라이브 URL 접속 확인: https://master.d3arok48ln7jvp.amplifyapp.com
- [ ] GitHub `D-W-Kim/CI-Course` push 권한 확인
- [ ] AWS IAM Access Key 별도 채널 수령
- [ ] AWS Amplify 콘솔 (`d3arok48ln7jvp`) 접근 확인
- [ ] `npm ci && npm run dev` 로컬 동작 확인
- [ ] `npm test` → 92/92 확인

### Day 2 (이해)

- [ ] `CLAUDE.md` · `CONTEXT.md` 정독
- [ ] `plan.md` 17 slices 확인
- [ ] `handoff.md` 최근 3개 entry 정독
- [ ] 라이브 URL에서 직접 워크시트 풀어보기

### Day 3 (DynamoDB 확인)

- [ ] `docs/dynamodb-setup.md` 확인
- [ ] `POST /api/rooms` 테스트 → 방 코드 생성 확인
- [ ] DynamoDB 콘솔에서 `cost-sim-rooms` 테이블 데이터 확인
- [ ] 2탭 시뮬레이션: 강사 방 생성 → 학습자 입장 → 라운드 진행

### Day 4~5 (강의 준비)

- [ ] D-2 리허설: 사내 노트북으로 라이브 URL 테스트
- [ ] Plan B 시나리오 확인 (DynamoDB 미동작 시)
- [ ] 1차수 강의 최종 리허설

### 🔑 보안 (인수 즉시)

- [ ] 이전 운영자(Sam)로부터 받은 Access Key → 사용 후 **즉시 삭제하고 새로 발급**
  - AWS 콘솔 → IAM → Users → `behappy6965` → Security credentials → Access keys → Delete → Create new

---

## 14. 참고 문서

| 문서 | 위치 | 용도 |
|------|------|------|
| **본 인수인계서** | `outputs/인수인계서-CI-Course-20260526.md` | 실무 인수용 (이 문서) |
| 상세 기술 인수인계서 | `projects/cost-sim-v2.1/docs/handover-sim-v2.1.md` | 개발자 딥다이브 (825줄) |
| 신규 계정 배포 가이드 | `projects/cost-sim-v2.1/docs/new-account-deploy-guide.md` | 처음부터 배포하는 사람용 |
| DynamoDB 설정 가이드 | `projects/cost-sim-v2.1/docs/dynamodb-setup.md` | DB 설정 전용 |
| 게임화 PRD | `outputs/cost-sim-v2.1-game-prd.md` | 기획 전체 스펙 |
| 학습자 설명서 | `outputs/개발원가-War-Game-설명서.pdf` | 강의장 배포물 |
| 운영 계약 | `CI-Course/CLAUDE.md` | 절대 금지 규칙 |

---

**긴급 연락**: Sam Ahn (원작자) — Telegram/슬랙 DM
**신규 라이브 URL**: https://master.d3arok48ln7jvp.amplifyapp.com
**GitHub**: https://github.com/D-W-Kim/CI-Course
